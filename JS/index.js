function login(){

		console.log("Validadon login");
		/*var conf=0;
		var usr = document.getElementById('inputUsuario').value;
		usr = usr.toLowerCase();
		var psw = document.getElementById("inputSenha").value;
		psw=psw.toLowerCase();
		if (usr=="admin" && psw=="admin") {
			window.location="../HTML/home.html";
			conf=1;
		}
		if (conf==0) { alert("Dados incorretos, tente novamente"); }*/
}	
